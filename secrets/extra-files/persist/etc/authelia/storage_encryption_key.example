# Raw secret value, one line, no KEY= prefix — Authelia reads this file
# directly (services.authelia.instances.main.secrets.storageEncryptionKeyFile).
# Generate a real one with: openssl rand -hex 32
REPLACE-ME-WITH-A-LONG-RANDOM-SECRET
