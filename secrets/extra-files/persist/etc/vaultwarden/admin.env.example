# Optional — without this, Vaultwarden still runs fine, it just has no
# /admin panel (no way to invite users, view diagnostics, etc.). Generate
# a real random token, e.g.:
#   openssl rand -base64 48
ADMIN_TOKEN='change-me-to-a-real-random-token'
