ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI... your-real-key-here beardedtek

# Copy this file to `authorized_keys` (no .example suffix, gitignored) in
# this same directory and replace the line above with your real public
# key. This file's path mirrors the target's filesystem layout exactly:
# nixos-anywhere's `--extra-files ./secrets/extra-files` copies it to
# /home/beardedtek/.ssh/authorized_keys on the deployed system.
